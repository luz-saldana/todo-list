# App name and description
The app is called My todos and displays a list of to-dos.

## Installation instructions
Once I cloned my repository, I typed in the terminal the following:
npm create vite@latest ./ -- --template react
Then install with the command: npm install

## How to run the development server
After installing, I issued: npm run dev 
Then I opened a browser with the local link provided